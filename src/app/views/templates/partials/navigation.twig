<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
                aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <!--<a class="navbar-brand" href="{{ path_for('index') }}">The Documentor</a>-->
            <a class="navbar-brand" href="{{ path_for('index') }}">
                <img alt="Brand" src="{{ base_url() }}/img/doc-logo.png">
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-left">
                <li class="active"><a href="{{ path_for('index') }}">Home <span class="sr-only">(current)</span></a></li>
                <li><a href="{{ path_for('guides.getAllGuides') }}">Explore</a></li>
            </ul>
            <form class="navbar-form navbar-left">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
            </form>
            {% if not auth.isLoggedIn %}
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path_for('auth.login') }}">Login</a></li>
                <li><a href="{{ path_for('auth.signup') }}" class="navbar-signup">Sign Up</a></li>
            </ul>
            {% else %}
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path_for('users.getAll') }}{{ auth.currentUser.username }}">Hello, <span class="text-primary">{{ auth.currentUser.username }}</span></a></li>
                <li><a href="{{ path_for('auth.logout') }}">Logout</a></li>
            </ul>
            {% endif %}
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>